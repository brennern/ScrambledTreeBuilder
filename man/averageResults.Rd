% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/averageResults.R
\name{averageResults}
\alias{averageResults}
\title{Average results}
\usage{
averageResults(df)
}
\arguments{
\item{df}{A results data frame produced by \code{\link[=formatStats]{formatStats()}} (therefore)
having a proper \code{lab} column.}
}
\value{
A tibble, even when the input is a data frame, because we use
\emph{dplyr}…
}
\description{
Most of the times we have two sets of alignment statistics for a pair of
genomes, where the \emph{target} and \emph{query} roles are exchanged.  Sometimes we
want to plot only one point per pair.  This function produces the averages.
}
\examples{
averageResults(Halo_DF)
}
\author{
Charles Plessy
}
