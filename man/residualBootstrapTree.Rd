% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrapTree.R
\name{residualBootstrapTree}
\alias{residualBootstrapTree}
\title{Residual-resampling bootstrap tree(s) from a distance matrix (UPGMA)}
\usage{
residualBootstrapTree(D, n_bootstrap = 1000L, clamp_zero = TRUE)
}
\arguments{
\item{D}{A square numeric distance matrix with identical row/column names, or a \code{dist} object.}

\item{n_bootstrap}{Integer number of bootstrap replicates (default 1000).}

\item{clamp_zero}{Logical; clamp negative distances in resampled matrices to 0 (recommended).}
}
\value{
A \code{list} with:
\itemize{
\item \code{tree}: Reference tree (\code{phylo}) built from \code{D} with UPGMA. Node labels contain bootstrap \%.
\item \code{bootstrap_trees}: List of replicate trees (\code{phylo}).
\item \code{support}: Numeric vector of bootstrap percentages per internal node (aligned to \code{tree$node.label}).
\item \code{call}: Matched call.
}
}
\description{
Build a reference tree from a distance matrix using UPGMA, then perform
residual-resampling bootstrap to obtain replicate trees and map clade
support onto the reference.
}
\examples{
out <- residualBootstrapTree(Halo_PercentDiff, n_bootstrap = 200)
plot(out$tree, show.node.label = TRUE)

}
\author{
Anika Mittal
}
