% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enr.R
\name{cladeENRtable}
\alias{cladeENRtable}
\title{Aggregate ENR scores by clade}
\usage{
cladeENRtable(tb_points, z_cut = 1, require_low_x = TRUE)
}
\arguments{
\item{tb_points}{A data.frame/tibble containing columns \verb{x,y,resid,ENRz,clade}
(and optionally \code{n}).}

\item{z_cut}{Threshold on standardized excess (\code{ENRz}) to tag categories (default 1).}

\item{require_low_x}{If \code{TRUE}, "ABOVE-TREND" must also have \code{x_mean} <= median \code{x_mean}.}
}
\value{
A tibble with one row per clade:
\code{clade}, \code{ENR} (mean raw residual), \code{ENRz} (mean standardized),
\code{x_mean}, \code{y_mean}, \code{n_nodes}, and \code{ENR_label}.
}
\description{
Summarise point-level ENR statistics at the clade level using weighted means
(weights = \code{n} when available). Optionally require that "ABOVE-TREND" clades
also sit on the lower-divergence side (\code{x_mean} <= median \code{x_mean}).
}
\examples{
# tb_enr <- MRCAs(df, focal) |> computeENR()
# cladeENRtable(tb_enr, z_cut = 1)

}
