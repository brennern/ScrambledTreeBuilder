% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractPercentID.R
\name{extractPercentID}
\alias{extractPercentID}
\title{Extract percent identities between species.}
\usage{
extractPercentID(node, tibble, matrix, fun = mean)
}
\arguments{
\item{node}{Node.}

\item{tibble}{Tibble data from dendrogram.}

\item{matrix}{Matrix created from the makeMatrix function.}

\item{fun}{Calculates the mean percent identity between nodes on a tree.}
}
\value{
Percent identity scores to store in a variable.
}
\description{
Extract percent identities between species.
}
\examples{
HClust <- hclust(dist(Matrix), method = "complete")
Dendrogram <- as.dendrogram(HClust)
TD <- tidytree::as_tibble(as.phylo(Dendrogram))
pIDs <- unique(TD$parent) |> sort() |> purrr::set_names() |> sapply(extractPercentID, TD, Matrix)
TD$percentID <- NA
TD[names(pIDs), "percentID"] <- unname(pIDs)
}
